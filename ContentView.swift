import SwiftUI

struct ContentView: View {
    @State var chanace = 0
    @State var answer = ""
    var body: some View {
       Text("Adam Kolacia")
       Text ("Louie")
        Button {
            
        } label: {
            Image("8ball 2")
                .frame(width: 300, height: 300)
        }

        }
    }

